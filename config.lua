Config = {
    QbcoreTemplate = true, -- this enables the template to be same as qb-core, if disabled it works for qbox.
    UseSingleResource = true, -- if yes, then it will use the fivem native GetResourcePath.
    ResourceName = "vehiclemeta" -- resourcename
    --ResourceName = "[stream]/[vehicles]"  -- also works with UseSingleResource false.
}